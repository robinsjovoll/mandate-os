# MandateOS: Create A Mandate

Use `mandateos_get_policy_catalog` first, then issue or choose a mandate before any risky OpenClaw action.

Prefer the `openclaw-guardrail` preset when the workflow needs shell execution, browser automation, or sub-agents.
